# radio-recognition-backend

Server for the radio recognition backend, run without ssl certificate using
`python3 server.py`
Or with using:
`python3 server.py [fullchain location] [private key location]`